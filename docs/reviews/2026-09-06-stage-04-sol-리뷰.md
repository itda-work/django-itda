# 4단계 규칙의 이사 — sol(Codex gpt-6-astra) 적대 리뷰 원문

- 일시: 2026-09-06 · 대상: `git diff stage-04-start stage-04-done`(bb1d060) · 실행: Herdr pane w8:p5, `codex --sandbox read-only`
- 처리: 아래 원문 뒤 "반영 결정" 절 참조

```
• 판정: 현재 상태를 4단계의 완료본으로 승인할 수 없다. mark_paid()의 DB 원자성은 맞지만, 환불의 부분 커밋·7일 경계 오류·실행 불가능한 대안과 보장 범위 과장이 남아 있다. 환불 중
  복·경합 해결은 5단계에 남겨야 한다.

  기존 테스트는 12개 모두 통과했다. 별도 메모리 DB 실측에서는 7일 23시간 환불 ALLOW, 순차 호출만으로 승인 환불 2건 생성, 주문 저장 실패 후 환불 approved / 주문 paid 잔존을 확인
  했다. 재고 부족 시에는 주문 pending과 기존 재고가 복원됐다. 코드와 저장소 파일은 수정하지 않았다.

   항목          1. mark_paid 원자성·순서
   판정          구현 통과, 설명 수정 필요. 상태 조건부 UPDATE와 모든 재고 차감이 같은 atomic() 안에 있고, 예외가 밖으로 전파돼 함께 롤백된다. 그러나 “순서가 뒤집히면 재고가 이
                 미 깎인다”는 설명은 틀렸다. 같은 트랜잭션에서 예외로 롤백하면 역순도 영구 차감을 남기지 않는다.
   근거 파일:줄  orders/models.py:79, 지시서:175 (stages/04-규칙의-이사.md:175)
   권고          현재 순서는 유지하고, 불필요한 재고 갱신 전에 전이를 거부하는 순서라고 설명하라. 뒤 상품에서 재고 부족이 발생했을 때 앞 상품 차감까지 복원되는 회귀 테스트를 추
                 가하라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          1. rowcount의 의미
   판정          판정은 맞고 진단 문구는 부정확하다. 0은 “대상 PK이면서 pending인 행이 없음”이다. 반드시 다른 요청이 먼저 성공했다는 뜻은 아니다. 또 self.status는 오래된 값일 수
                 있어 거부 이유에 “현재 결제 대기”가 나올 수 있다.
   근거 파일:줄  orders/models.py:80, 지시서:171 (stages/04-규칙의-이사.md:171)
   권고          실패 이유를 조건 불일치로 표현하라. 재조회하지 않은 메모리 값을 DB의 현재 상태라고 표시하지 마라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          1. 환불 TOCTOU·중복
   판정          있다. 분리 자체보다 적용 경계의 부재가 문제다. apply()는 내부에서 판정하지만 주문을 잠그거나 상태를 조건부 갱신하지 않는다. 두 요청 모두 승인될 수 있고, 동시성
                 없이 두 번 호출해도 중복 승인된다. 고액 제안 이후 실제 승인 시점에는 기한도 다시 검사하지 않는다.
   근거 파일:줄  orders/models.py:201, orders/models.py:249, orders/models.py:260, agent/views.py:145
   권고          중복·상태 경합 해결은 5단계로 예약하되, 4단계에서는 이를 보장하지 않는다고 명시하라. 기한을 신청 시점에 판정하는지 승인 시점까지 지켜야 하는지도 지금 정의하라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          1. 환불 커밋 원자성
   판정          즉시 수정해야 할 결함. 환불 생성 → 승인 저장 → 주문 취소 저장이 따로 커밋된다. 마지막 저장에 실패하면 승인 환불과 미취소 주문이 공존한다. 실측으로 확인했다.
   근거 파일:줄  orders/models.py:253, orders/models.py:270, orders/views.py:28
   권고          자동 승인 전체와 직접 approve() 호출 모두에 원자성을 보장하라. 트랜잭션 추가만으로 중복 환불까지 해결되는 것은 아니다.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          1. SQLite·스레드 테스트
   판정          중복 차감 방지의 보조 검사로만 인정. rowcount 경합 판정의 증명으로는 불합격. 시작 동기화가 없어 순차 실행도 통과한다. 모든 OperationalError를 locked로 삼고 결과
                 개수·패자의 종류를 검사하지 않는다. SELECT 단계 오류도 같은 취급이다. SQLite 잠금 실패는 도메인 DENY가 아니다.
   근거 파일:줄  테스트:194 (tests/stage_04_transition_contract.py:194), 테스트:206 (tests/stage_04_transition_contract.py:206), 테스트:224 (tests/
                 stage_04_transition_contract.py:224), agent/views.py:168
   권고          지금은 테스트 주장과 예외 처리를 좁혀라. 두 결과 수집과 예상 밖 예외 실패를 확인하라. 동기화된 경합·잠금 재시도·HTTP 응답 검증은 5단계에서 다뤄라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          2. 시작 결함·고정 fixture
   판정          의도적으로 준비한 결함이지만 보장 부재의 결정적 반례로 유효하다. 다만 “세 법 모두 프롬프트에만 있다”는 설명은 사실과 다르다. 시작 코드에는 이미 5만원 임계에 따
                 른 자동 승인·격상이 있다. 또한 실패 11개 중 7개는 계약 모듈 부재로 먼저 실패하므로, 실패 개수 전체를 실제 법 위반 실측으로 셀 수 없다.
   근거 파일:줄  stage-04-start:orders/models.py:157, 지시서:24 (stages/04-규칙의-이사.md:24), 지시서:79 (stages/04-규칙의-이사.md:79), 테스트:48 (tests/
                 stage_04_transition_contract.py:48), 설계:37 (docs/단계별-설계.md:37)
   권고          **“일부만 코드화한 교육용 시작 상태”**라고 소개하라. 카드 ⑤⑥의 상태·재고 변화는 행동 증거, 모듈·대장 검사는 제출 계약 검사로 구분하라. LLM의 실제 불복종을 관찰
                 했다고 쓰지 마라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          2. 힌트·핵심 문장·확정 주체
   판정          유도 실습으로 적정하나 독립 설계 평가로는 힌트가 과하다. 타입·상수·분기·SQL까지 주어진다. 핵심 문장은 유지할 가치가 있지만 Django·DB 도구가 선언한 조건과 지정
                 경로 안에서 지킨다는 한정이 필요하다. “확정은 사람”도 소액 자동 승인과 그대로는 양립하지 않는다.
   근거 파일:줄  지시서:124 (stages/04-규칙의-이사.md:124), 지시서:154 (stages/04-규칙의-이사.md:154), 지시서:215 (stages/04-규칙의-이사.md:215), 설계:11 (docs/단계별-설
                 계.md:11)
   권고          힌트를 단계적으로 공개하고 경계·롤백 이유를 설명하게 하라. **“사람이 승인한 정책 범위는 규칙이 자동 확정하고, 격상 건은 사람이 확정한다”**로 관통 문장을 보완하
                 라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          3. 규칙 대장 시행 형식
   판정          분류 자체는 대체로 정직하다. 보장 범위 설명은 부족하다. REFUND-001은 DB 제약이 아니며 직접 approve()로 우회된다. REFUND-002의 권한 검사는 모델이 아니라 뷰·admin
                 에 있다. ORDER-001도 mark_paid() 경로의 계약이다. pending만 허용하는 구현은 원문의 “paid 재결제 금지”보다 강하다.
   근거 파일:줄  RULES.md:8, RULES.md:9, RULES.md:10, orders/views.py:25, orders/admin.py:61
   권고          학생은 애플리케이션 검증과 DB 제약의 차이를 반드시 알아야 한다. 대장에 적용 경로·우회 가능 경로를 명시하고, 권한 시행 위치도 링크하라. 원문보다 강화한 전이 정책
                 은 해석·합의로 표시하라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          3. append-only
   판정          이번 diff는 준수하지만 구조적 보장은 없다. 기존 행의 상태 변경을 허용하므로 엄밀한 불변 로그도 아니다. 현재 검사는 ID 문자열 존재만 확인해 원문 덮어쓰기·중복 버
                 전·후속 버전 누락을 잡지 못한다.
   근거 파일:줄  RULES.md:3, RULES.md:20, 테스트:275 (tests/stage_04_transition_contract.py:275)
   권고          **“삭제 금지, 내용 보존, 상태만 제한적으로 갱신하는 버전 대장”**이라고 정의하라. 기준 커밋 대비 행 보존·ID 유일성·후속 버전 연결 검사로 규약을 지켜라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          4. 존재하지 않는 alternatives
   판정          계약 위반. 즉시 제거해야 한다. 기한 초과는 환불 객체도 생성하지 않는데 “예외 승인 요청(격상)”을 약속한다. 교환·재발송 경로도 없다. “5만원 이하 분할 환불”은 승인
                 회피를 권장하며 현재 API에는 분할 금액 입력 경로도 없다. 테스트가 이 잘못된 대안을 요구한다.
   근거 파일:줄  orders/models.py:219, orders/models.py:229, orders/models.py:250, 테스트:154 (tests/stage_04_transition_contract.py:154), 거부 화면:23 (agent/templates/agent/
                 denied.html:23)
   권고          DENY에는 실제 가능한 조회·종료 경로만 제공하라. 고액에는 기존 승인 큐를 안내하라. 실행 가능한 대안이 없으면 빈 목록을 허용하고, 테스트도 비어 있지 않음보다 실행
                 가능성과 정책 준수를 검증하라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          4. 판정과 확정의 의미
   판정          계약 내부가 모순된다. 아무것도 쓰지 않는 decide()가 “규칙이 확정한다”고 반환한다. 기존 고액 환불을 사람이 승인한 뒤 다시 요청해도 decide()를 재호출해 ESCALATE를
                 돌려줄 수 있다. 판정과 현재 처리 상태가 섞였다.
   근거 파일:줄  orders/verdict.py:5, orders/models.py:202, orders/models.py:237, agent/views.py:151
   권고          ALLOW는 허용 판정, 커밋 성공은 별도 결과로 정의하라. 기존 객체는 실제 처리 상태를 응답하라. 완전한 멱등 응답 저장은 5단계에 남겨도 된다.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          5. 이사 비용표
   판정          앱 코드 수치는 정확하지만 효율 주장은 성립하지 않는다. 앱 5파일 +270 −47, migration +18은 맞다. 전체 diff는 **11파일 +360 −55**다. 문서는 +69 −5, 테스트 변경은
                 +3 −3인데 표는 문서 +16, 테스트는 start 포함으로만 표시한다. 시작에 이미 들어간 테스트 282줄의 준비 비용도 빠진다.
   근거 파일:줄  비용표:22 (stages/README.md:22), 비용표:31 (stages/README.md:31), 비용표:48 (stages/README.md:48), 설계:56 (docs/단계별-설계.md:56)
   권고          구현差分과 교재·fixture 준비 비용을 분리하고 재현 가능한 집계 명령을 제공하라. LOC는 변경량이지 시간·유지비·효율의 증명이 아니다. “shell로 와도 항상 지켜진다”는
                 삭제하라.
  ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   항목          6. 다음 단계 진입 조건
   판정          단일 요청의 정확성과 설명은 지금 고치고, 중복·경합은 예약한다. 설계가 5단계에 멱등키·경합·공유 세계를 명시했으므로 이를 앞당겨 완성하면 실습 소재가 사라진다. 반
                 대로 4단계 완료를 “실접속에 필요한 보장이 완성됨”으로 읽히게 해서는 안 된다.
   근거 파일:줄  설계:38 (docs/단계별-설계.md:38), 설계:48 (docs/단계별-설계.md:48), 지시서:250 (stages/04-규칙의-이사.md:250)
   권고          아래 경계로 수정 범위를 고정하라. 실접속은 미보장 범위를 명시한 관찰 트랙으로 설명하라.

  최우선 수정 3개

  1. 환불 커밋을 원자적으로 묶어라. 자동 승인과 직접 승인 모두 실패 시 환불·주문이 함께 복원돼야 한다. approved / paid 잔존을 막는 실패 주입 테스트를 추가하라. 근거: orders/
     models.py:253, orders/models.py:270.

  2. 7일 규칙을 정확히 구현하라. .days > 7은 8일째부터 거부한다. 실측한 7일 23시간 ALLOW는 “7일 초과 금지” 위반이다. 정확한 시간 차를 비교하고 7일 직전·정확히 7일·직후를 검사하
     라. 생성일을 결제일 대신 쓰는 교육상 가정과 판정 기준 시점도 명시하라. 근거: orders/models.py:207, 지시서:157 (stages/04-규칙의-이사.md:157).

  3. Verdict의 허위 약속을 없애라. 미구현 예외 승인·교환·재발송과 승인 회피용 분할 안내를 제거하고, 이를 강제하는 테스트를 수정하라. ALLOW를 커밋 완료와 구분하라. 근거: orders/
     models.py:219, 테스트:154 (tests/stage_04_transition_contract.py:154), orders/verdict.py:5.

  5단계 전에 반드시 고칠 것

  - 위 최우선 3개와 기존 객체의 실제 처리 상태 응답.
  - rowcount·롤백 순서 설명, 스레드 테스트의 과도한 증명 주장. 근거: 지시서:171 (stages/04-규칙의-이사.md:171), 테스트:212 (tests/stage_04_transition_contract.py:212).
  - “세 법 모두 자연어에만 존재”, “항상 지켜진다”, 비용표 누락과 확정 주체의 모순. 근거: 설계:37 (docs/단계별-설계.md:37), 비용표:31 (stages/README.md:31), 설계:11 (docs/단계별-
    설계.md:11).

  - 대장의 보장 범위·버전 보존 규약. 또한 “한 단계 법 하나·약 200줄 이내” 원칙에 대한 4단계 예외를 명시하라. 현재는 법 3개와 앱 코드 추가 270줄이다. 근거: RULES.md:3, 설계:15
    (docs/단계별-설계.md:15).

  지금 고치지 말아야 할 예약된 결함

  - 환불 재전송·동시 제안의 중복 승인, 멱등키와 유일성 제약: 5단계 핵심 소재로 유지하라. 근거: 설계:38 (docs/단계별-설계.md:38), agent/views.py:149.
  - 판정 이후 주문 상태 변경·승인 경합의 직렬화, 잠금 실패 처리, 공유 세계: 미보장임을 공개하고 5단계에서 해결하라. 단일 요청의 부분 커밋과 구분해야 한다. 근거: orders/
    models.py:260, 설계:38 (docs/단계별-설계.md:38).

  - paid_at 도입과 actor·판정·규칙 버전을 남기는 감사 장부: 7단계 예약을 유지하라. 현재 가정을 명시하는 것으로 4단계 범위를 제한하라. 근거: orders/models.py:207, 설계:40 (docs/
    단계별-설계.md:40).

  - 선언 기반 도구 생성·세계 부채 자동 검사: 8단계에 남겨라. 이번에는 보장 없는 경로를 정직하게 기록하는 데 그쳐라. 근거: 설계:41 (docs/단계별-설계.md:41).



```

## 반영 결정 (fable 비판 재검토, 2026-09-06)

리뷰 총평 "4단계 완료본으로 승인 불가"를 수용 — 수정 라운드 후 `stage-04-done` 태그를 수정 커밋으로 **이동**한다(학생의 start→done diff에 수정이 포함되어야 함). `stage-04-start`는 불변.

| # | 리뷰 항목 | 결정 | 비고 |
|---|---|---|---|
| 1 | 환불 커밋 원자성(생성→승인→주문 취소 분리 커밋) | **수용·즉시** | 단일 요청 결함. 실패 주입 테스트 추가 |
| 2 | 7일 경계 `.days` 절삭(7일 23시간 ALLOW) | **수용·즉시** | 정확한 시간 차 비교. 판정 기준 시점 = **신청 시점**으로 정의(승인 시점 재검사는 5단계 경합 소재) |
| 3 | 허위 alternatives(미구현 격상·교환·분할 회피) | **수용·즉시** | DENY는 실제 가능한 경로만(빈 목록 허용), ESCALATE는 승인 큐 안내. "비어 있지 않음" 테스트 제거 |
| 4 | ALLOW(판정) vs 커밋 성공 구분, 기존 객체는 실제 상태 응답 | 수용·최소 | `apply()` 반환에 outcome 추가. 완전 멱등 응답은 5단계 |
| 5 | rowcount 의미·롤백 순서 설명 오류, `self.status` 오래된 값 노출 | 수용 | 지시서·docstring·거부 사유 문구 교정 |
| 6 | 스레드 테스트 과잉 주장 | 수용 | 결과 2개 수집·예상 밖 예외 실패로 좁힘. 동기화 경합은 5단계 |
| 7 | "세 법 모두 프롬프트에만" 부정확 · "항상 지켜진다" 과장 · 확정 주체 모순 | **수용** | 관통 문장 보완: **"사람이 승인한 정책 범위는 규칙이 자동 확정하고, 격상 건은 사람이 확정한다"** — 규칙 대장의 출처(점주)가 그 승인의 물증. 설계서 §0·§3 4단계 행 수정 |
| 8 | 규칙 대장: 보장 범위·우회 경로 명시, ORDER-001 원문보다 강함(해석 표시), ID 유일성·행 보존 검사 | 수용 | 열 추가 대신 "적용 경로/우회 가능 경로" 비고 열 1개 + 해석 표시. 테스트는 ID 유일·기존 행 원문 보존 단언 |
| 9 | 비용표: 구현 diff vs 준비 비용 분리, 재현 명령, 효율 주장 삭제 | 수용 | 4단계 "한 단계 법 하나·200줄" 예외 명시(법 3개·+270) |
| 10 | 힌트 과다(타입·상수·분기·SQL까지 제시) | **보류** | 초보 대상 유도 실습이 설계 의도. 단, "왜 이 순서·왜 롤백" 설명 요구 문항 1개 추가 |
| 11 | 5단계 예약 결함(중복 승인·동시 제안·잠금 재시도·paid_at·부채 검사) | 수용 | 고치지 않음. 지시서 말미 "이 단계가 보장하지 않는 것" 절로 공개 |
