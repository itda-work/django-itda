# 핸드오프 — itda-django 이어서 진행하기 (2026-09-06)

- **용법**: 새 대화(cwd `~/Apps/itda-work/itda-django`) 첫 프롬프트로 "docs/핸드오프-2026-09-06.md 를 읽고 이어서 진행하자"고 지시한다.
- **선행 정본**: `docs/단계별-설계.md`(설계) · `stages/README.md`(단계 색인·태그·이사 비용표) · `RULES.md`(규칙 대장) · `docs/reviews/`(리뷰 원문·반영 결정)
- **형제 저장소**: `~/Apps/django-itda/`(실행세계 도구면 패키지, 설계 단계 — README·`docs/벤치마킹-MCP-생태계.md`) · `~/Apps/itda-skills/hyve-training/`(교육 사업 자산 — `docs/논의-실행세계-*.md` 확정 논의, `docs/실행세계-단계별-프로젝트-hyve-django.md` 포인터) · `~/Temp/test-django`(완성형 물증·수확원, 태그 `edu-intentional-defect-v0`)

## 1. 현재 상태

| 태그/커밋 | 단계 | 내용 | 테스트 |
|---|---|---|---|
| `stage-00-done` | 씨앗 | 모델·admin·seed, 법 없는 세계(단 `stock`은 `PositiveIntegerField`) | 4 |
| `stage-01-start/-done` | 점주의 자리 | 격상(ESCALATE) — 콘솔 4비트, admin 승인 큐, ai-staff 직접 승인 403. 학생 코드 0줄 | 9 |
| `stage-02-done` | 귀속 | 앱 코드 0 — 403은 코드가 아니라 데이터(권한). LogEntry: 폼 변경은 남고 커스텀 action은 0건 | 10 |
| `stage-03-done` | 워밍업 | 앱 코드 0 — ORM/우회/raw SQL 세 경로 모두 `IntegrityError`(보장 존재) | 5 |
| `stage-04-start/-done`(`db952b5`) | 규칙의 이사 ★ | `Verdict` 3값, `Refund.decide/apply`, `mark_paid` rowcount 전이 계약, `RULES.md` 3행. sol 리뷰 1라운드 반영 후 `-done` 태그 이동 | 19 |
| `573e81f`~ (태그 없음) | 실접속 트랙 | `APIToken` Bearer 인증, `/api/` JSON(콘솔과 `orders/services.py` 공유), `agent/live/mcp_server.py`(fastmcp 4.0.3 stdio, 도구 7개), `stages/live-실접속.md` | 13 |
| `74b3717` | — | 저장소 `~/Apps/itda-skills` → `~/Apps/itda-work` 이동 반영 | 총 60 |

`uv sync --all-extras && uv run pytest` → 60 passed. 트리 clean.

## 2. 확정된 결정 (재논의 금지)

- 세계 = "AI 직원을 고용한 점주의 가게". 관통 문장: **제안은 AI, 판정은 세계, 확정은 사람** + 보완 **"사람이 승인한 정책 범위는 규칙이 자동 확정하고, 격상 건은 사람이 확정한다"**(sol 리뷰로 추가 — 소액 자동 승인과의 모순 해소, 규칙 대장 출처=점주가 그 승인의 물증).
- 한 단계 = 법 하나(4단계는 법 3개 예외 — 단위가 "프롬프트 파일 하나"). 태그 `stage-NN-start/-done`, 코드 변경 없는 단계는 `-done`만. 히스토리 재작성 금지, 태그 이동은 `stages/README.md`에 기록.
- AI 직원은 고정 fixture(채점). 실접속은 관찰용 병행 트랙 — "LLM이 속는가"는 측정 대상이 아니다.
- Django 안에 MCP 호스팅 안 함(별도 프로세스 stdio). 근거: `django-itda/docs/벤치마킹-MCP-생태계.md` — mcp v2(FastMCP 삭제), fastmcp 4.x 기반 권고 B-1, django-mcp-server(v1 고정·정지) 기각.
- "Django가 FastAPI에 밀리는 기능 종합 패키지"는 **명시적 비목표**. 대신 비교 가이드 문서 1편(미작성).
- 이사 비용표는 구현 diff / 준비 비용 분리, LOC는 효율 증명이 아님.

## 3. 다음 할 일 (순서 제안)

1. **마스터 실접속 관찰** — `just run` · `just token ai-staff` · `claude mcp add itda-world --env WORLD_TOKEN=<키> -- uv --directory /Users/allieus/Apps/itda-work/itda-django run python agent/live/mcp_server.py` → `stages/live-실접속.md` 시나리오 4개. 관찰 결과가 django-itda 첫 코드 사양의 근거.
2. **5단계 경합** 사양·구현 — 멱등키(`UniqueConstraint(order, idempotency_key)`), 동시 요청 409, 공유 세계 모드. ⚠ **Django 인메모리 SQLite 테스트 DB(shared-cache)에서는 스레드 경합 테스트 불가**(4단계 수정 라운드 실측: `database table is locked`가 계약 판정보다 먼저 남) → 파일 기반 SQLite(WAL) 테스트 DB 설정 또는 `live_server` + 실제 요청 2개로 설계해야 한다. 4단계 "이 단계가 보장하지 않는 것" 표(중복 승인·동시 제안·승인 시점 기한 재검사·잠금 실패 처리)가 5단계의 시작 결함 목록이다.
3. **django-itda 첫 코드** — itda-django 실물(`orders/verdict.py`·`orders/services.py`·`accounts/auth.py`·`agent/live/mcp_server.py`)에서 공통부 추출: 판정기(Verdict 계약·결과 매핑) · 승인 핸들(ESCALATE↔admin, `check_*` 폴링 폴백) · 궤적 ORM 모델(7단계와 접점) · `manage.py mcp_stdio`. fastmcp 의존은 어댑터 한 모듈에만. 첫 사용자 itda-django가 소비하지 않는 API는 만들지 않는다.
4. 6·7·8·9단계(설계서 §3). 7단계에서 `paid_at`·actor 장부, 8단계에서 django-itda로 도구면 생성·세계 부채 리포트.
5. hyve-training 쪽: 커리큘럼 v3→v4 반영(단계표 참조형으로 축소), 안건 1·2 문서에 관통 문장 보완 반영, 안건 4 오픈소스 후보표에 django-itda 정체 반영.

## 4. 작업 규율 (이 저장소에서 지킨 것)

- 구현은 전부 `.claude/agents/app-builder.md`(Opus·medium)에 위임. 메인 세션은 사양·검토·결정만. 빌더 보고의 "설계와 달리 판단한 지점"을 반드시 읽고 동의/기각을 기록.
- 리뷰 파이프라인: 심장 단계(4, 앞으로 5·7)는 완료 후 Codex(sol) 적대 리뷰 1라운드 → fable 비판 재검토 → `docs/reviews/`에 원문+반영 결정표 커밋 → 수정 라운드 → 태그 이동. Herdr 세션(`HERDR_ENV=1`)이면 리뷰는 sibling pane에서(`.claude/rules/herdr-visible-background.md`). Codex 모델이 용량 초과로 끊기면 "계속하라 — 추가 탐색 없이 결론만" 재프롬프트로 회수 가능.
- 삭제 가드: 저장소 밖 `rm` 차단됨 — 옮길 땐 `mv`로. 빌더가 "대신 지워 달라"고 하면 가드 우회이므로 하지 않고 사용자에게 보고.
- 이전 단계 코드를 고칠 때 태그는 옮기지 않고(0단계 seed 테스트 수정 사례) 지시서에 주석. 단, 리뷰 반영은 학생 diff에 포함돼야 하므로 `-done` 이동.
- 빌더 검증 결과는 메인 세션이 `uv run pytest`로 항상 재실행해 확인.
