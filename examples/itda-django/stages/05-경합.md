# 5단계 — 경합

4단계 마지막 문장을 들고 왔다.

> 오늘 쓴 rowcount 판정이 5단계에서 다시 나온다.

다시 나오기는 하는데, **한 번은 버티고 한 번은 못 버틴다.** 이번 단계는 그 차이를
보는 곳이다. 그리고 그 차이 때문에, 처음으로 법을 **DB 제약**으로 내려 놓는다 —
3단계에서 "전부 지켜지는 층"이라고 이름만 붙여 놓았던 그 칸이다.

## 준비 — 4단계가 못 막는다고 적어 둔 것들

```bash
just reset-db
cat agent/prompts/ai_staff.md
```

문장이 하나 늘었다.

> 4. **같은 환불을 두 번 처리하지 마라. 한 주문에 환불은 한 번이다.**

이사 표시가 없다. 아무도 안 지켜 준다는 뜻이다.

4단계 지시서의 [「이 단계가 보장하지 않는 것」](04-규칙의-이사.md#이-단계가-보장하지-않는-것)
표에서 "5단계"라고 적어 둔 행을 다시 읽는다. 넷이다.

| 못 막는 것 | 4단계가 적어 둔 지금 일어나는 일 |
|---|---|
| 같은 주문에 환불 두 번 신청 | 승인된 환불이 두 개 생긴다(콘솔이 걸러 줄 뿐) |
| 동시에 들어온 두 제안 | 둘 다 승인될 수 있다. 잠그지 않는다 |
| 승인 시점의 기한 재검사 | 신청 때만 본다 |
| DB 잠금 실패 | `OperationalError` 가 그대로 올라간다 |

오늘의 결과를 미리 항목별로 적어 둔다. **"5단계에서 다 해결됨"이라고 뭉뚱그리지
않는다** — 무엇이 어떻게 해결됐는지가 항목마다 다르기 때문이다.

| 항목 | 오늘의 판정 |
|---|---|
| 중복 생성 | **DB 제약이 막는다.** 경로 무관 — shell 에서 `create()` 해도 걸린다 |
| 동시 확정·승인 뒤 거부 | **전이 계약이 막는다.** `approve`/`reject` 경로에서만 시행된다 |
| 저장 판정 재생(멱등) | **열쇠를 보내고 행이 생긴 요청만.** DENY·ALREADY 는 저장하지 않는다 |
| 잠금 실패 응답 | **503 + `Retry-After` 로 번역한다.** 잠금 자체를 없앤 것이 아니다 |
| 승인 시점 기한 재검사 | **하지 않기로 정했다.** 판정 시점은 신청 시점이다 |

마지막 행이 왜 "미구현"이 아니라 "정책"인지도 오늘의 내용이다.

## 명령 — 같은 요청 두 개를 동시에 쏜다

이번 단계의 도구는 콘솔 카드가 아니다. 같은 요청 두 개를 **정확히 같은 순간에**
출발시키는 명령이다.

```bash
just race refund SEED-0003 --amount 30000
```

```
[A] 200 ALLOW  COMMITTED · 기한 안(1일 경과)이고 50,000원 이하다 …
[B] 200 ALLOW  COMMITTED · 기한 안(1일 경과)이고 50,000원 이하다 …
```

**승인된 환불이 두 건이다.** 한 주문에 3만원씩 두 번 나갔다. 확인해 본다.

```bash
uv run python manage.py shell -c "from orders.models import Refund; print(list(Refund.objects.values_list('order__order_number','status','amount')))"
```

이번엔 결제다.

```bash
just race pay SEED-0004
```

```
[A] 200 ALLOW  SEED-0004 을(를) 결제 완료로 옮겼습니다.
[B] 409 DENY [ORDER-001@v1]  ORDER-001@v1: 결제 대기 상태가 아니다(조건 불일치). …
```

**이건 버틴다.** 한정판 재고 1개도 한 번만 깎였다.

채점표도 같은 말을 한다.

```bash
just test 05     # 21개 중 15개가 실패한다
```

통과하는 여섯 개는 **성격이 셋으로 갈린다.** 섞어서 세면 안 된다.

| 통과하는 테스트 | 왜 시작 상태에서도 통과하나 |
|---|---|
| `test_동시_결제_두_개_중_하나만_통과한다_HTTP` | **이미 켜진 법** — 4단계 rowcount 계약이 UPDATE 경합을 버틴다 |
| `test_4단계_세_행은_ID_원문_출처까지_그대로다` | **이미 켜진 법** — 대장 세 행은 그대로여야 한다 |
| `test_select_for_update는_SQLite에서_아무것도_잠그지_않는다` | **실측** — 코드가 아니라 백엔드의 성질을 잰다 |
| `test_테스트_DB는_파일이다` | **준비 확인** — 이게 아니면 경합 시험이 무엇을 쟀는지 모른다 |
| `test_스키마_오류는_잠금이_아니다_500` | **회귀 방지** — 아직 503 미들웨어가 없으니 500 이 맞다. 오늘 만들 미들웨어가 이걸 깨면 안 된다 |
| `test_열쇠_없이_기한이_지난_뒤_다시_신청하면_지금_상태를_듣는다` | **회귀 방지** — 지금은 서비스 층의 사전 조회가 우연히 같은 답을 준다. 그 조회를 걷어낼 때 답이 나빠지면 안 된다 |

첫째가 오늘의 소재다. 4단계에서 못 했던 **경합의 증명**을 오늘은 할 수 있고,
4단계 계약은 그 증명을 통과한다.

마지막 행은 특히 눈여겨본다. 시작 상태의 사전 조회는 **순차 요청에 대해서는
맞는 답을 준다.** 그래서 오늘 그 줄을 건드릴 때, "틀린 코드를 지운다"가 아니라
"맞는 답을 유지하면서 막는 일만 옮긴다"가 목표가 된다.

## 의도적 실패 — 왜 결제는 버티고 환불은 못 버티는가

두 요청의 모양이 다르다.

- 결제는 **UPDATE** 다. `WHERE status = 'pending'` 을 걸 **행이 이미 있다.**
  두 스레드가 같은 행에 조건부 UPDATE 를 쏘면 DB 가 줄을 세우고, 진 쪽은
  rowcount 0 을 받는다. 4단계에 심은 계약 그대로다.
- 환불 제안은 **INSERT** 다. 아직 아무 행도 없다. **조건을 걸 대상이 없다.**

그럼 4단계는 중복을 어떻게 막고 있었나. `orders/services.py` 를 연다.

```python
existing = Refund.objects.filter(order=order).exclude(status=Refund.Status.REJECTED).first()
if existing:
    return existing.current_outcome()
return Refund.apply(...)
```

**조회하고, 없으면 만든다.** 이름이 있는 패턴이다 — *check-then-act*.
두 요청이 순서대로 오면 잘 동작한다. 동시에 오면 **둘 다 조회를 통과한다.**
조회와 생성 사이에 벌어진 그 틈이 오늘의 자리다. 4단계 주석에 이미 적혀 있었다 —
"세계가 중복을 막아 주는 것이 아니다. 여기서 걸러 줄 뿐이고, 그 얘기는 5단계다."

확정 쪽에도 같은 틈이 있다. `orders/api.py:refund_approve` 를 연다.

```python
if refund.status != Refund.Status.PROPOSED:
    ...
refund.approve(request.user)
```

역시 **조회하고 나서 쓴다.** 그리고 `Refund.approve` 자체는 조건 없는 `save()` 다.
`reject()` 도 마찬가지다 — 이미 승인된 환불을 거부로 **덮어쓴다.** 그러면 주문은
취소된 채로 환불은 '거부'가 되고, 장부가 거짓말을 한다.

```bash
just race approve <환불 ID>     # 둘 다 200 이다
```

마지막으로 하나 더. 잠금 실패다.

> 세계가 잠겨서 판정하지 **못한** 것과, 판정해서 **거부한** 것은 다른 사건이다.

지금은 `OperationalError` 가 그대로 올라가 **500** 이 된다. AI 직원이 500 을 받으면
무엇을 해야 하는지 알 수 없다. 다시 보내야 하는가, 포기해야 하는가.

## 수정 — 세 개의 경합, 세 개의 도구

법은 **하나**다.

> `REFUND-003@v1` — 같은 환불을 두 번 처리하지 마라. 한 주문에 환불은 한 번이다.

시행 면이 셋인 이유는 경합의 모양이 셋이기 때문이다. 한 문장의 세 얼굴이다.

| 경합의 모양 | 막는 것 | Django 가 준 자리 |
|---|---|---|
| 다른 두 요청이 같은 주문에 환불을 만든다 (INSERT) | 살아 있는 환불은 주문당 하나 | `UniqueConstraint(condition=…)` → `IntegrityError` |
| 같은 요청이 두 번 온다 (재전송) | 같은 열쇠는 같은 답 | `UniqueConstraint(order, idempotency_key)` + 저장해 둔 판정 |
| 점주 둘이 동시에 확정한다 (UPDATE) | 확정은 제안 상태에서만 | 4단계 모양 그대로 — 조건부 UPDATE + rowcount |

세 가지는 **한 문장에서 자동으로 나오지 않는다.** 전부 우리가 고른 것이다.

- **무엇이 "같은 사건"인가** — 열쇠(`Idempotency-Key`)가 같으면 같은 요청으로 본다.
  금액이 달라도 최초 내용이 이긴다. 충돌을 400 으로 되돌려주는 세계도 있다.
- **거부 뒤의 새 제안은 중복인가** — 아니라고 봤다. 그래서 제약의 조건절에
  `rejected` 를 넣지 않았다. 넣었다면 한 번 거부된 주문은 영영 환불이 막힌다.
- **열쇠는 재사용되는가** — 만료도 정리도 없다. 영원히 남는다.

원문 한 줄에서 이 셋이 연역되지 않는다. **정책을 골라서 코드에 박은 것**이고,
그래서 `RULES.md` 의 "적용 경로" 열에 무엇이 어디까지인지를 적는다.

### 0. 먼저 세계를 다시 심는다

**코드를 고치기 전에 해야 한다.** 위 관찰이 남긴 것은 같은 주문의 승인 환불
2건이고, 그 상태에서 새 제약을 올리면 마이그레이션이 실패한다.

```bash
# 1. 관찰 결과를 적어 둔다 (아래 명령의 출력을 제출물에 붙인다)
uv run python manage.py shell -c "from orders.models import Refund; print(list(Refund.objects.values_list('order__order_number','status','amount')))"

# 2. 세계를 다시 심는다
just reset-db

# 3. 그 다음에 코드를 고치고
# 4. 마지막에 마이그레이션
uv run python manage.py makemigrations orders && uv run python manage.py migrate
```

2번을 건너뛰면 4번에서 이렇게 멈춘다.

```
django.db.utils.IntegrityError: UNIQUE constraint failed: orders_refund.order_id
```

새 제약은 **이미 있는 행에도** 적용된다. 실습이 만든 중복 2건이 그 제약을 못
넘는 것이고, 마이그레이션은 거기서 멈추는 것이 맞다 — 통과시켰다면 그 제약은
거짓말이다.

운영이라면 지울 수 없다. 오염된 데이터를 살리면서 제약을 올리는 절차
(expand → 정리 → contract, 마이그레이션 3개)는 **7단계**에서 원가까지 세어 가며
직접 해 본다. 여기서는 교육용 세계라 다시 심는 것으로 넘어간다.

순서대로 심는다.

### 1. 제약 — `orders/models.py:Refund.Meta`

```python
constraints = [
    models.UniqueConstraint(
        fields=['order'],
        condition=Q(status__in=['proposed', 'approved']),
        name='refund_one_live_per_order',
    ),
    models.UniqueConstraint(
        fields=['order', 'idempotency_key'],
        condition=~Q(idempotency_key=''),
        name='refund_idempotency_key_per_order',
    ),
]
```

**부분** 유일이라는 점이 중요하다. `fields=['order']` 만 걸면 거부된 건이 있는
주문에는 영영 환불을 못 올린다. 거부는 사건의 **끝**이고, 그 뒤의 새 제안은
같은 환불의 중복이 아니라 **다른 사건**이다. 조건절이 그 정의를 담는다.

두 번째 제약의 `~Q(idempotency_key='')` 도 같은 종류의 판단이다. 열쇠를 안 준
요청 둘을 "같은 요청"이라고 부를 근거가 없으므로, 빈 열쇠는 제약 밖에 둔다.

이건 3단계의 `CHECK ("stock" >= 0)` 와 **같은 층**이다. 확인해 본다.

```bash
uv run python manage.py shell -c "
from orders.models import Refund, Order
o = Order.objects.get(order_number='SEED-0003')
Refund.objects.create(order=o, amount=1, reason='shell', status='approved')
Refund.objects.create(order=o, amount=1, reason='shell 두 번째', status='approved')
"
```

`IntegrityError`. 서비스 함수도, 판정도, 뷰도 지나지 않았는데 걸렸다.
4단계의 세 법은 여기서 아무것도 못 했다.

### 2. `Refund.apply` — 조회의 역할을 바꾼다

4단계에도 조회가 있었다. 지우는 것이 아니라 **역할을 바꾼다.**

```python
# 4단계 — 조회가 막는다 (막지 못한다)
existing = Refund.objects.filter(order=order).exclude(status=REJECTED).first()
if existing:
    return existing.current_outcome()
return Refund.apply(...)

# 5단계 — 조회는 답하고, 막는 것은 제약이다
known = cls._existing_outcome(order, idempotency_key)   # 이미 있는 사건이면 그 답
if known is not None:
    return known
verdict = cls.decide(...)                                # 없을 때만 판정한다
try:
    with transaction.atomic():
        refund = cls.objects.create(...)                 # 최종 방어는 여기 걸린 제약
except IntegrityError as collision:
    return cls._resolve_collision(order, idempotency_key, collision)
```

조회는 그대로 있는데 **틀린 것은 조회가 아니라 조회에 걸었던 기대**였다.
같은 코드가 두 가지 일을 할 수 있다.

| 조회에 무엇을 맡기나 | 되는가 |
|---|---|
| **중복을 막는다** | 안 된다. 경합에서 두 요청이 조회를 나란히 통과한다 |
| **이미 답이 있는 질문에 답한다** | 된다. 답은 이미 DB 에 있고, 읽어서 주면 된다 |

그리고 **조회가 판정보다 앞에 있어야 한다.** 뒤에 두면 이런 일이 난다 —
점주가 승인까지 끝낸 건에 같은 열쇠로 재전송했더니, 그 사이 7일이 지났다는
이유로 "기한 초과라 거부합니다"(409)가 돌아온다. 이미 환불된 건을 신규 요청처럼
재판정한 것이다. **일어난 일은 일어난 일이고, 재전송은 그 사실을 다시 묻는
것이지 새로 허가를 구하는 것이 아니다.**

조회를 통과해 버린 요청은 어디서 걸리나. INSERT 다.

```python
except IntegrityError as collision:
    return cls._resolve_collision(order, idempotency_key, collision)
```

`_resolve_collision` 은 `_existing_outcome` 을 **다시** 부른다. 조회했을 때와
같은 두 갈래로 답해야 하기 때문이다 — 늦게 도착했다는 이유로 다른 말을
들으면, 같은 요청이 타이밍에 따라 다른 답을 받는 셈이다.

| 상황 | 돌려주는 것 | `Outcome` |
|---|---|---|
| 같은 열쇠의 행이 있다 | 그때 저장해 둔 판정 | `REPLAYED` |
| 살아 있는 환불이 있다 | 지금 상태 | `ALREADY` |
| 둘 다 아니다 | 원 예외를 그대로 올린다 | — |

두 가지를 지킨다.

- `IntegrityError` 는 **`atomic()` 블록 바깥에서** 잡는다. 안에서 잡고 계속
  쓰면 이미 깨진 트랜잭션 위에 쿼리를 날리게 된다.
- 분류는 **기존 행의 존재**로 한다. 예외를 뜯어 어느 제약에 걸렸는지 보지
  않는다 — SQLite 의 `IntegrityError` 메시지에는 제약 이름이 실리지 않아
  판별이 불안정하다. 그래서 계약을 좁혀 적는다: *기존 행이 있으면 그 행으로
  답하고, 없으면 원 예외를 그대로 올린다.* 기존 행이 있는 상황에서 일어난
  별개의 무결성 오류는 이 분류에 가려진다는 것도 함께 적어 둔다.

#### 재생의 범위 — 무엇이 "같은 답"인가

`REPLAYED` 로 돌아오는 것은 **저장된 판정과 그 판정의 HTTP 코드**다.
함께 실리는 객체 상태(`refund.status`)는 **지금 값**이다. 그래서 승인이 끝난
건에 재전송하면 `202 ESCALATE` + `status: approved` 라는 조합이 나온다.
"처음과 글자 하나 다르지 않은 응답"이 아니다 — 판정은 그때의 것이고,
세계의 현재는 현재의 것이다.

저장하지 **않는** 것도 분명히 해 둔다.

- **DENY** — 아무것도 저장하지 않는다. 그래서 같은 열쇠로 다시 와도 재생이 아니라
  **현재 조건으로 새 판정**이다. 동일한 본문은 보장하지 않는다(경과 시간이
  사유 문장에 들어가므로 글자 단위로는 대개 달라진다).
- **ALREADY** — 이 답도 저장하지 않는다. 답을 만든 사건이 없기 때문이다.
  열쇠를 실어 보냈어도 그 열쇠는 어디에도 안 남는다.

### 3. `Refund.approve` / `reject` — 4단계 모양 그대로

```python
moved = Refund.objects.filter(pk=self.pk, status=Status.PROPOSED).update(...)
if not moved:
    raise InvalidTransition(...)
```

`Order.mark_paid` 와 한 글자도 다르지 않은 모양이다. 그리고 4단계의 규율도
그대로 따른다 — 거부 사유에 `self.status` 를 찍지 않는다. 재조회하지 않은
메모리 위의 값을 DB 의 현재 상태인 양 말할 수 없다.

부르는 쪽 셋이 이 예외를 받는다.

| 부르는 곳 | 받으면 |
|---|---|
| `orders/views.py` (콘솔) | `messages.warning` + 되돌아가기 |
| `orders/admin.py` (승인 큐 액션) | `n건 승인, m건은 제안 상태가 아니어서 건너뜀` |
| `orders/api.py` (JSON) | **409** + `rule_ids: ['REFUND-003@v1']` |

`orders/api.py:refund_approve` 에서 **`status != PROPOSED` 사전 검사를 지운다.**
그 세 줄이 4단계에 남아 있던 check-then-act 였다.

### 4. 멱등키 입구 — `orders/api.py`

`Idempotency-Key` 헤더를 먼저 보고, 없으면 본문의 `idempotency_key` 를 본다.
멱등키는 요청 **내용**이 아니라 요청 **자체**에 붙는 표찰이라 헤더가 제자리다.

응답의 상태 코드는 손대지 않는다. `REPLAYED` 면 저장된 판정을 그대로 들고
오므로 `verdict.status_code` 가 처음 응답과 같아진다 — 재전송이 처음과 같은
답을 받는다는 것은 본문뿐 아니라 **상태 코드까지** 같다는 뜻이다.

MCP 도구에도 같은 열쇠 구멍을 낸다. `propose_refund(..., request_id=...)` 가
그 값을 헤더로 옮긴다.

### 5. 잠금 실패는 판정이 아니다 — `config/middleware.py`

```python
def process_exception(self, request, exception):
    if not isinstance(exception, OperationalError):
        return None
    ...  # 503 + Retry-After: 1
```

- **409 로 만들면 안 된다.** 규칙 위반이 아닌데 규칙 위반이라고 답하는 것이다.
- **500 으로 두어도 안 된다.** 받은 쪽이 재시도해도 되는지 알 수 없다.
- 본문에 `kind`·`rule_ids` 를 싣지 않는다. **판정한 적이 없기 때문이다.**

`OperationalError` 가 아니거나 메시지에 `locked`·`busy` 가 없으면 손대지 않는다.
500 은 500 이다.

설정도 함께 만진다.

```python
'OPTIONS': {'transaction_mode': 'IMMEDIATE', 'timeout': 5,
            'init_command': 'PRAGMA journal_mode=WAL;'}
```

**이건 완화이지 보장이 아니다.** 잠금 실패의 빈도를 줄일 뿐 없애지 못한다.
없앨 수 없는 것을 정직하게 말하는 응답이 보장이다.

### `select_for_update` — 잠갔다고 믿는 잠금

경합이라고 하면 제일 먼저 떠오르는 것이 잠금이다. 써 본다.

```python
with transaction.atomic():
    list(Order.objects.select_for_update().filter(status='paid'))   # 평가까지 해야 SQL 이 나간다
```

오류도, 경고도 없다. 그런데 **`FOR UPDATE` 가 붙지 않는다.**

```python
connection.features.has_select_for_update   # False
```

Django 는 백엔드가 지원할 때만 `FOR UPDATE` 를 붙인다. SQLite 는 지원하지
않으므로 평범한 SELECT 가 나간다. 코드는 행을 잠갔다고 말하고 있고, 세계는
그 행을 안 잠갔다.

**"아무 잠금도 없다"는 뜻은 아니다.** 위에서 켠 `IMMEDIATE` 트랜잭션 잠금은
그대로 걸린다 — 다만 그건 행 단위가 아니라 **DB 단위**이고, `select_for_update`
가 약속하는 것과는 다른 층이다. 층을 구분하지 않으면 "잠금은 걸려 있으니
괜찮다"는 잘못된 안심이 남는다.

`QuerySet` 을 평가하지 않으면 SELECT 조차 안 나간다는 것도 함께 본다.
실행되지 않은 코드는 무엇도 증명하지 않는다.

PostgreSQL 이면 진짜 행 잠금이 걸린다. 그건 여기서 확인할 수 없다 —
이 과정은 8단계까지 외부 서비스 0 이다. 그래서 오늘 배우는 것은
"어느 잠금을 쓰나"가 아니라 **"내가 쓴 보장이 실제로 켜져 있는지 확인하는 법"**이다.

## 검증

```bash
just test 05
uv run pytest            # 0~4단계·실접속도 그대로 통과해야 한다
```

이사 전후를 나란히 놓는다.

| 명령 | 이사 전 | 이사 후 |
|---|---|---|
| `just race refund SEED-0003 --amount 30000` | `COMMITTED` / `COMMITTED` — **승인 환불 2건** | `COMMITTED` / `ALREADY` — 1건 |
| `just race pay SEED-0004` | 200 / 409 (그대로) | 200 / 409 (그대로) |
| `just race approve <ID>` | 200 / 200 | 200 / **409** `REFUND-003@v1` |

같은 열쇠로 두 번 보내 본다.

```bash
curl -s -X POST http://127.0.0.1:8000/api/refunds/ \
  -H "Authorization: Bearer $WORLD_TOKEN" \
  -H "Idempotency-Key: k1" -H 'Content-Type: application/json' \
  -d '{"order_number": "SEED-0002"}' | python3 -m json.tool
```

두 번 다 202 이고, `refund.id` 가 같고, 두 번째의 `outcome` 이 `REPLAYED` 다.
**행은 하나뿐이다.**

## 이 단계의 한 문장

> **물어본 답으로 막지 마라. 막는 것은 제약이다.**

조회를 지운 것이 아니다. 조회는 그대로 있고, 오히려 판정보다 **앞으로** 왔다.
바뀐 것은 조회에 걸었던 기대다 — 막는 일은 DB 에 맡기고, 조회는 "이미 답이 있는
질문에 정직하게 답하는" 일만 한다. 4단계에서 rowcount 로 배운 것과 같은 종류의
발상인데, 이번에는 조건을 걸 행이 없어서 **제약**이라는 다른 도구를 썼다.

그리고 4단계의 관통 문장에 한 겹이 붙는다.

> 제안은 AI, 판정은 세계, 확정은 사람. 그리고 **같은 사건은 두 번 일어나지 않는다.**
> 재시도는 새 사건이 아니고(멱등키), 경합의 패자는 거짓말 없이 "이미 있다"를 듣는다.

## 이 단계가 보장하지 않는 것

| 못 막는 것 / 안 하기로 한 것 | 지금 일어나는 일 | 어디서 |
|---|---|---|
| 승인 시점의 기한 재검사 | **정책으로 확정: 하지 않는다.** 판정 시점은 신청 시점이다. 기한은 사건의 성질이지 경합이 아니다 | 바꾸려면 `REFUND-001@v2` — 새 행이 필요한 운영 판단이다 |
| 실제 잠금 실패의 재현 | 채점하지 않는다(환경 의존). 503 은 **주입** 테스트로만 확인한다. `IMMEDIATE`·`timeout`·WAL 은 빈도를 줄일 뿐 없애지 못한다. PostgreSQL 에서는 재현된다(실측 2026-09-19, 채점표 밖 `tests/pg_probe.py`) — 다른 연결이 행을 쥔 동안 `select_for_update(nowait=True)` 는 `OperationalError`(SQLSTATE `55P03`)로 돌아오고 `django_itda.busy.is_lock_failure` 가 잠금으로 알아본다. 그 실패가 503 까지 번역되는 경로는 PG 에서도 주입으로만 본다 | — |
| 재고 마지막 1개를 **다른 두 주문**이 동시에 결제 | 초과 판매는 `stock__gte` 조건부 UPDATE(0단계 범용 법 + 4단계)가 막는다. 그러나 **막는 것과 답하는 것은 별개다** — `services.pay_order()` 는 `InvalidTransition` 만 판정으로 번역하고 `InsufficientStock` 은 그대로 올려 보내 **500** 이 된다. 재고 부족은 판정할 수 있는 사건인데 아직 판정 형식이 없다 | 번역은 8단계 — 운영 |
| DENY 재전송 | 최초 DENY 는 저장하지 않으므로 재요청 시 **현재 조건으로 새 판정**한다. **동일 본문은 보장하지 않는다** — 경과 시간이 사유 문장에 들어가므로 글자 단위로는 대개 달라진다. 요청 장부를 두면 재생할 수 있지만 이번 단계에 넣지 않았다 | 7단계 — 궤적 |
| 같은 열쇠에 다른 내용 | 검사하지 않는다. **최초 내용이 이긴다**(표찰이 같으면 같은 요청이다). 충돌을 400 으로 되돌려주는 것도 가능한 정책이지만 고르지 않았다 | 고른 정책 |
| 멱등키의 만료·정리 | 없다. 영원히 남는다 | 8단계 — 운영 |
| 열쇠·상태를 우회한 직접 조작 | DB 가 지키는 것은 **현재 행의 부분 유일성**이다. 행을 지우거나 `update(status='rejected')` 로 상태를 바꾸면 같은 주문에 환불을 또 올릴 수 있다 — "평생 한 번"이 아니다 | 7단계 — 궤적 |
| 누가 재전송했는가 · 경합의 패자가 누구였는가 | 아무 데도 안 남는다. 결과 상태로만 짐작한다 | 7단계 — 궤적 |
| 남의 주문에 환불을 거는 것 | 소유권 스코핑이 없다 | 6단계 — 주체 |

## 실접속으로 같은 것을 보기

Claude 창을 **둘** 띄우고 같은 말을 동시에 시킨다.

> "SEED-0003 을 3만원 환불해 줘"

하나는 확정되고 하나는 "이미 있다"를 듣는다. 둘 다 성공했다고 말하지 않는다.

같은 창에서 두 번 시키면 도구가 `request_id` 를 붙였는지에 따라 갈린다 —
붙였으면 `outcome: REPLAYED`(그때의 답), 안 붙였으면 `ALREADY`(지금 상태).
**둘 다 정직한 답이고, 둘은 다른 답이다.**

옆자리 학생과 해 보려면 세계를 열어야 한다.

```bash
just run-shared                                  # 0.0.0.0 바인드 + WORLD_SHARED=1
just race refund SEED-0003 --http --url http://<상대 IP>:8000 --rounds 10
```

`--http` 는 **확률적**이다. 겹치는 창이 좁아 매번 경합하지 않는다. 그래서 회차를
반복하고, **회차마다 새 주문을 접수·결제해** 대상을 새로 만든다(`SEED-0003` 은
장바구니 원본으로만 쓴다). 같은 주문에 계속 쏘면 2회차부터는 이미 처리된 건이
있어서 경합과 무관하게 `ALREADY` 가 나온다 — 그러면 회차를 세는 의미가 없다.

**`ALREADY` 하나만으로는 경합의 증거가 못 된다.** 순차로 두 번 보내도 `ALREADY`
다. 경합을 봤다고 말하려면 두 요청이 같은 창에서 출발했다는 것까지 알아야 하고,
그걸 보장하는 것이 in-process 모드의 Barrier 다. `--http` 는 "진짜 네트워크에서도
같은 답이 나온다"를 보는 자리이지 경합의 증명이 아니다.

`WORLD_SHARED=1` 이 여는 것은 호스트 검사까지다. 학생별 계정과 소유권 스코핑은
6단계의 몫이다.

## 제출물

`stages/제출물/05-<본인이름>.md` 로 만들어 낸다.

### 1. `RULES.md` 행 1개

`REFUND-003@v1` 행을 그대로 붙인다. "적용 경로 / 우회 가능 경로" 열에서
**면마다 보장 범위가 다르다**는 점을 확인한다.

### 2. 이사 비용 — 한 줄

[단계 색인의 이사 비용표](README.md#이사-비용표--법-하나의-원가를-줄-수로)에 있는
재현 명령으로 뽑는다. 손으로 세지 않는다.

### 3. 설명 — 왜 다른 도구인가

> **UPDATE 경합과 INSERT 경합은 왜 다른 도구로 막는가?
> 그리고 `select_for_update` 를 SQLite 에서 썼다면 무엇이 달라졌겠는가?**

두 번째 질문의 답은 **아무것도**다. 그게 왜 위험한지까지 자기 말로 쓴다.

### 4. 마지막 한 줄

`just race refund` 를 이사 전에도 이사 후에도 실행했다. **명령은 같았다.**
달라진 것을 한 문장으로 쓴다.

## 다음

6단계에서는 **누구의 주문인가**를 묻는다. 지금 `list_orders` 는 권한 하나로
모든 고객의 주문을 보여 준다 — 오늘 심은 제약도 그건 막지 않는다.
