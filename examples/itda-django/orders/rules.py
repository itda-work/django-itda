"""도메인 법 — 우리 가게에만 있는 규칙의 선언.

`agent/prompts/ai_staff.md` 의 자연어 문장이 여기로 이사 왔다.
Django 는 이 법을 써주지 않았다. 어디에 쓸지와 지켜줄 방법만 줬다.

숫자를 상수로 빼는 이유는 예뻐서가 아니라 **버전을 붙이기 위해서**다.
7일이 10일로 바뀌면 `REFUND-001@v2` 가 생기고 `@v1` 은 deprecated 가 된다.
그때 "언제부터 10일이었나"에 답할 수 있어야 한다.

여기 있는 ID 는 전부 `RULES.md`(규칙 대장)에 같은 이름의 행을 갖는다.
"""

# --- 임계값 -------------------------------------------------------------------

REFUND_WINDOW_DAYS = 7
ESCALATE_OVER = 50_000
# 결제 링크의 수명(초). 이 숫자가 **법의 정본**이다 — 1시간이 2시간으로 바뀌면
# `PAY-001@v2` 가 생긴다. `config/settings.py:PAYMENT_LINK_TIMEOUT` 은 관찰용 노브이지
# 법이 아니다(그쪽은 "1시간을 기다리지 않기" 위한 스위치다).
PAY_LINK_TIMEOUT = 60 * 60

# --- 규칙 ID ------------------------------------------------------------------

REFUND_001 = 'REFUND-001@v1'
REFUND_002 = 'REFUND-002@v1'
ORDER_001 = 'ORDER-001@v1'
REFUND_003 = 'REFUND-003@v1'
PAY_001 = 'PAY-001@v1'
SCOPE_001 = 'SCOPE-001@v1'

# 원문 — 사람이 말한 그대로. 코드 요약이 아니다.
RULE_TEXTS = {
    REFUND_001: '결제 후 7일이 지난 주문은 환불하지 마라.',
    REFUND_002: '5만원을 초과하는 환불은 반드시 점주 승인을 받아라.',
    ORDER_001: '이미 결제 완료된 주문을 다시 결제 완료로 만들지 마라.',
    REFUND_003: '같은 환불을 두 번 처리하지 마라. 한 주문에 환불은 한 번이다.',
    PAY_001: (
        '결제는 네가 하지 마라. 고객에게 결제 링크를 안내하고 고객이 직접 결제하게 하라. '
        '링크는 발급 후 1시간 안에만 유효하다.'
    ),
    SCOPE_001: '고객의 주문은 그 고객에게만 보여 줘라.',
}
